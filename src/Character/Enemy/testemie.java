// This is just example code

package Character.Enemy;
import Character.Player;

public class testemie extends Enemy {

    public testemie(Player player, String name, int occupationID, int level, int x, int y, String message) {
        super(player, name, occupationID, level, x, y, message);
    }
}